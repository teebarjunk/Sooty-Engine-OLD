shader_type canvas_item;
render_mode blend_mix;

uniform float h;
uniform float v;

void fragment() {
	vec2 px = SCREEN_PIXEL_SIZE;
	vec4 col = vec4(0);
	
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-36.0, v*-36.0)) * 0.004208;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-35.0, v*-35.0)) * 0.004601;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-34.0, v*-34.0)) * 0.005017;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-33.0, v*-33.0)) * 0.005458;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-32.0, v*-32.0)) * 0.005922;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-31.0, v*-31.0)) * 0.00641;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-30.0, v*-30.0)) * 0.006921;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-29.0, v*-29.0)) * 0.007453;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-28.0, v*-28.0)) * 0.008007;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-27.0, v*-27.0)) * 0.008579;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-26.0, v*-26.0)) * 0.00917;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-25.0, v*-25.0)) * 0.009777;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-24.0, v*-24.0)) * 0.010398;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-23.0, v*-23.0)) * 0.01103;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-22.0, v*-22.0)) * 0.011672;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-21.0, v*-21.0)) * 0.01232;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-20.0, v*-20.0)) * 0.012971;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-19.0, v*-19.0)) * 0.013622;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-18.0, v*-18.0)) * 0.014271;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-17.0, v*-17.0)) * 0.014912;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-16.0, v*-16.0)) * 0.015543;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-15.0, v*-15.0)) * 0.016161;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-14.0, v*-14.0)) * 0.01676;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-13.0, v*-13.0)) * 0.017339;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-12.0, v*-12.0)) * 0.017892;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-11.0, v*-11.0)) * 0.018417;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-10.0, v*-10.0)) * 0.018909;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-9.0, v*-9.0)) * 0.019366;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-8.0, v*-8.0)) * 0.019784;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-7.0, v*-7.0)) * 0.02016;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-6.0, v*-6.0)) * 0.020492;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-5.0, v*-5.0)) * 0.020777;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-4.0, v*-4.0)) * 0.021014;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-3.0, v*-3.0)) * 0.021199;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-2.0, v*-2.0)) * 0.021333;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*-1.0, v*-1.0)) * 0.021413;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*0.0, v*0.0)) * 0.02144;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*1.0, v*1.0)) * 0.021413;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*2.0, v*2.0)) * 0.021333;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*3.0, v*3.0)) * 0.021199;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*4.0, v*4.0)) * 0.021014;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*5.0, v*5.0)) * 0.020777;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*6.0, v*6.0)) * 0.020492;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*7.0, v*7.0)) * 0.02016;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*8.0, v*8.0)) * 0.019784;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*9.0, v*9.0)) * 0.019366;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*10.0, v*10.0)) * 0.018909;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*11.0, v*11.0)) * 0.018417;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*12.0, v*12.0)) * 0.017892;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*13.0, v*13.0)) * 0.017339;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*14.0, v*14.0)) * 0.01676;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*15.0, v*15.0)) * 0.016161;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*16.0, v*16.0)) * 0.015543;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*17.0, v*17.0)) * 0.014912;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*18.0, v*18.0)) * 0.014271;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*19.0, v*19.0)) * 0.013622;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*20.0, v*20.0)) * 0.012971;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*21.0, v*21.0)) * 0.01232;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*22.0, v*22.0)) * 0.011672;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*23.0, v*23.0)) * 0.01103;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*24.0, v*24.0)) * 0.010398;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*25.0, v*25.0)) * 0.009777;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*26.0, v*26.0)) * 0.00917;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*27.0, v*27.0)) * 0.008579;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*28.0, v*28.0)) * 0.008007;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*29.0, v*29.0)) * 0.007453;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*30.0, v*30.0)) * 0.006921;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*31.0, v*31.0)) * 0.00641;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*32.0, v*32.0)) * 0.005922;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*33.0, v*33.0)) * 0.005458;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*34.0, v*34.0)) * 0.005017;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*35.0, v*35.0)) * 0.004601;
	col += texture(SCREEN_TEXTURE, SCREEN_UV + px * vec2(h*36.0, v*36.0)) * 0.004208;
	
	col.a = 1.0;
	COLOR = col;
}